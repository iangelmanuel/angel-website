---
import { ClientRouter } from "astro:transitions"

import ThemeScript from "@/components/seo/ThemeScript.astro"
import { Toaster } from "@/components/ui/sonner"

import BaseHead from "@/components/seo/BaseHead.astro"
import PreloadFont from "@/components/seo/PreloadFont.astro"
import JsonLd from "@/components/seo/JsonLd.astro"
import TopMenu from "@/components/shared/TopMenu.astro"
import MainContent from "@/components/shared/MainContent.astro"
import Footer from "@/components/shared/Footer.astro"

import { useI18n } from "@/i18n"
import {
  faqLd,
  organizationLd,
  professionalServiceLd,
  servicesLd,
  webSiteLd
} from "@/libs/seo"

import "@/modules/portfolio/styles/portfolio.css"

interface Props {
  title?: string
  description?: string
}

const { title, description } = Astro.props

const { lang, design } = useI18n(Astro)
---

<ThemeScript />

<html
  lang={lang}
  data-design={design}
  class="scroll-smooth"
>
  <head>
    <BaseHead
      title={title}
      description={description}
    >
      <PreloadFont
        slot="preloadfonts"
        design={design}
      />
    </BaseHead>

    <JsonLd
      id="ld-organization"
      data={organizationLd(lang)}
    />
    <JsonLd
      id="ld-website"
      data={webSiteLd(lang)}
    />
    <JsonLd
      id="ld-business"
      data={professionalServiceLd()}
    />
    <JsonLd
      id="ld-services"
      data={servicesLd(lang)}
    />
    <JsonLd
      id="ld-faq"
      data={faqLd(lang)}
    />

    <ClientRouter />
  </head>

  <body>
    <TopMenu />

    <MainContent>
      <slot />
    </MainContent>

    <Toaster
      position="top-right"
      richColors
      duration={3000}
      client:visible
    />

    <Footer />
  </body>
</html>
