---
import { getCollection } from "astro:content"

import Project from "../../components/Project.astro"

import { ui, useI18n } from "@/i18n"

const { t, lang } = useI18n(Astro)
const projects = t(ui.projects)

const projectsCollection = await getCollection("projects", (project) => {
  const [projectLang] = project.id.split("/")
  return projectLang === lang && project.data.featured
})

const projectsFilteredByDate = projectsCollection.sort((a, b) => {
  return (
    new Date(b.data.publishDate).getTime() -
    new Date(a.data.publishDate).getTime()
  )
})
---

<section
  id="projects"
  class="compact-section projects-band py-10"
>
  <div class="container-width text-center">
    <span class="section-badge">{projects.badge}</span>

    <h2 class="section-title text-center">{projects.title}</h2>

    <div class="grid grid-cols-1 gap-4 sm:gap-6 md:grid-cols-2">
      {
        projectsFilteredByDate.map(({ id, data }, index) => (
          <Project
            id={id}
            data={data}
            index={index}
          />
        ))
      }
    </div>
  </div>
</section>
